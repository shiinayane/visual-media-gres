# Working, pinned versions for the Blackwell/aarch64 + CUDA 13 server.
# PyTorch MUST come from the cu128 wheel index (the default PyPI aarch64 wheel is
# CPU-only). Install with:
#   pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
# Detectron2 is built from source (see setup.sh) -- it is NOT on PyPI for this stack.
#
# torch==2.7.1+cu128         # installed separately from the cu128 index
# torchvision==0.22.1+cu128  # installed separately from the cu128 index

numpy==1.26.4          # <2: pycocotools / detectron2 0.6 need the numpy-1.x ABI
scipy==1.17.1
transformers==4.40.2
tokenizers==0.19.1
timm==1.0.27
opencv-python==4.11.0.86
pycocotools==2.0.11
Pillow==10.4.0
shapely==2.1.2
h5py==3.16.0
scikit-image==0.26.0
cython==3.2.5
fvcore==0.1.5.post20221221
iopath==0.1.10
omegaconf==2.3.1
matplotlib==3.11.0
tabulate==0.10.0
termcolor==3.3.0
yacs==0.1.8
ftfy==6.3.1
regex==2026.5.9
gdown==5.2.0
huggingface-hub==0.36.2
ninja
